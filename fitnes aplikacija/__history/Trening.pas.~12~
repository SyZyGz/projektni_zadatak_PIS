unit Trening;

interface

uses
  System.SysUtils, System.Types, System.UITypes, System.Classes, System.Variants,
  FMX.Types, FMX.Controls, FMX.Forms, FMX.Graphics, FMX.Dialogs, FMX.Layouts,
  FMX.Objects, FMX.Controls.Presentation, FMX.StdCtrls, FMX.ExtCtrls;

type
  TForm3 = class(TForm)
    MaleWorkout: TImage;
    FemaleWorkout: TImage;
    CustomWorkout: TImage;
    Vezbe Za Muskarce: TButton;
    procedure CustomWorkoutClick(Sender: TObject);
    procedure VezbeZaMuskarceClick(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  Form3: TForm3;

implementation

{$R *.fmx}

procedure TForm3.CustomWorkoutClick(Sender: TObject);
begin

end;

procedure TForm3.VezbeZaMuskarceClick(Sender: TObject);
begin

end;

end.
